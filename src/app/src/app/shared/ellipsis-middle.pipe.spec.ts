import { EllipsisMiddlePipe } from './ellipsis-middle.pipe';

describe('EllipsisMiddlePipe', () => {
  it('create an instance', () => {
    const pipe = new EllipsisMiddlePipe();
    expect(pipe).toBeTruthy();
  });
});
