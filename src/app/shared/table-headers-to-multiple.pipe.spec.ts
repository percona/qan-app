import { TableHeadersToMultiplePipe } from './table-headers-to-multiple.pipe';

describe('TableHeadersToMultiplePipe', () => {
  it('create an instance', () => {
    const pipe = new TableHeadersToMultiplePipe();
    expect(pipe).toBeTruthy();
  });
});
