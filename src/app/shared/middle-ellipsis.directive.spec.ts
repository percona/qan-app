import { MiddleEllipsisDirective } from './middle-ellipsis.directive';

describe('MiddleEllipsisDirective', () => {
  it('should create an instance', () => {
    const directive = new MiddleEllipsisDirective();
    expect(directive).toBeTruthy();
  });
});
