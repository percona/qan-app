import { PmmTooltipDirective } from './pmm-tooltip.directive';

describe('PmmTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new PmmTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
