import { LoadBarChartDirective } from './load-bar-chart.directive';

describe('LoadBarChartDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadBarChartDirective();
    expect(directive).toBeTruthy();
  });
});
