{
  "name": "percona-summary-wrapper",
  "version": "1.0.0",
  "description": "Percona Summary panel for Grafana",
  "main": "module.js",
  "scripts": {
    "build": "rm -rf dist/*; tsc && cp plugin.json dist/; cp -r src/img/ dist/img/; cp -r src/*.html dist/; cp -r src/dashboards/ dist/dashboards/",
    "clean": "rm -rf dist/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "percona",
    "pt-summary",
    "pt-mysql-summary",
    "pt-mongo-summary",
    "grafana"
  ],
  "author": "askomorokhov",
  "license": "AGPL",
  "dependencies": {
    "typescript": "^2.3.4"
  }
}
