{
  "name": "qan-app-wrapper",
  "version": "1.0.0",
  "description": "Percona Query Analytics panel for Grafana",
  "main": "module.js",
  "scripts": {
    "build": "rm -rf dist/*; tsc && cp plugin.json dist/; cp -r src/img/ dist/img/; cp -r src/*.html dist/; cp -r src/dashboards/ dist/dashboards/",
    "clean": "rm -rf dist/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "percona",
    "qan",
    "grafana"
  ],
  "author": "askomorokhov",
  "license": "AGPL",
  "dependencies": {
    "typescript": "^2.3.4"
  }
}
